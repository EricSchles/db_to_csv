from distutils.core import setup

setup(
    name="db_api",
    version="0.0",
    description="a high level query engine to bring database tables locally for further inspection.",
    author="Eric Schles",
    packages=["db_api"]
)
